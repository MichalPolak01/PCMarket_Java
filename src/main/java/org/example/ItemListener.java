package org.example;

/**
 * This interface represents an item listener for handling item click events.
 */
public interface ItemListener {
    /**
     * This method is called when an item is clicked.
     *
     * @param item The item that was clicked.
     */
    public void onClickListener(Item item);
}
